version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: mongo_ecommerce
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ecommerce_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
